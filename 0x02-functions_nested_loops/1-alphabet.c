#include "main.h"
/**
 * main - entry point
 * print_alphabet - function for alphabet
 * return: Always 0
 */
void print_alphabet(void);
int main(void);
{
	print_alphabet();
	return (0);
}
void print_alphabet(void)
{
	char letter = 'a';
	while(a <= z)
	{
	_putchar(letter);
	letter++
	}
	_putchar('\n');
}
